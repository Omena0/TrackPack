To run the exploit, host an http server here.

`python3 -m http.server 8000` or `py -m http.server 8000`

run `gen_packs.py` to generate resource packs. Copy the resulting packs.json to `run/plugins/TrackPack/packs.json`.